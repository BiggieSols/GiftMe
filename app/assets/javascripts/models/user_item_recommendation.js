GiftMe.Models.UserItemRecommendation = Backbone.Model.extend({
  urlRoot: "/user_item_recommendations",
  // toJSON: function() {
  //   return { user_item_recommendation: _.clone( this.attributes ) };
  // }
});